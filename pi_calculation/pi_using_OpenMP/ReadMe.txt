This version uses openMP parallelization to estimate pi using the monte carlo method . The results can be obtained using the python 3 script script1.py, the which performs two processes. As a conclusion from the tests of the script, it appears that this method requires significantly more time in relation to the pthreads.
 
RUN SCRIPT : pyhton3 script script1.py